/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'utaboiwoepvwdhnczsgd.supabase.co',
            },
        ],
    },
};

export default nextConfig;
